I’m ready to help implement the Zod input validation, but I can’t explore or modify the repository because I don’t have access to its files or a way to run commands in the environment. If you can share the relevant code (e.g., API routes and data structures that need validation), I can guide you on creating the Zod schemas, integrating them at API boundaries, and adding appropriate tests.